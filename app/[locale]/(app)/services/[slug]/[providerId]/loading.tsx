import { ProviderProfileSkeleton } from "@/components/services/skeletons";

/** Shown between tapping a card and the profile resolving. */
export default function Loading() {
  return <ProviderProfileSkeleton />;
}
